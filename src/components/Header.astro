---
import navData from '@data/navData.json';
import { Icon } from 'astro-icon/components';
import DarkModeToggle from '@components/DarkModeToggle.astro';

// TODO: eliminar comentario de prueba

// Filter navigation data to show admin menu only when enabled
// TODO: esto me pide unificarlo en un sólo sitio ya que está repetido tanto en el Header como en el Footer
const showAdminMenu = import.meta.env.PUBLIC_ADMIN_MENU === 'true';
const filteredNavData = navData.filter(entry => {
  if (entry.isAdmin) {
    return showAdminMenu;
  }
  return true;
});
---

<!-- ============================================ --><!--                 Navigation                   --><!-- ============================================ -->
<header class="c-navigation" data-js="navigation">
  <div class="c-container">
    <!--Nav Logo-->
    <a href="/" class="c-logo" aria-label="back to home">
      <img
        src="/assets/images/escudo.png"
        alt="Escudo de Belmontejo"
        width="210"
        height="29"
        decoding="async"
      />
      <span class="c-logo-text">Ayuntamiento de Belmontejo</span>
    </a>
    <!--Navigation List-->
    <nav class="c-nav" role="navigation">
      <!--Mobile Nav Toggle-->
      <button
        class="c-toggle"
        data-js="mobile-menu-toggle"
        aria-expanded="false"
        aria-controls="expanded-menu"
        aria-label="Toggle mobile menu."
      >
        <div class="c-box" aria-hidden="true">
          <span class="c-line c-line1" aria-hidden="true"></span>
          <span class="c-line c-line2" aria-hidden="true"></span>
          <span class="c-line c-line3" aria-hidden="true"></span>
        </div>
      </button>
      <!-- If you want to use another navigation stitch and enjoy the benefits of dynamic routing, replace the .cs-ul-wrapper in the navigation stitch with this one below -->
      <div class="c-list-wrapper">
        <ul id="expanded-menu" class="c-list">
          {
            filteredNavData.map(entry => (
              <li
                class:list={[
                  'c-list__item',
                  { 'c-dropdown': entry.children?.length > 0 },
                ]}
              >
                {entry.children?.length > 0 ? (
                  // If entry has children in navData.json, create a button and a dropdown icon
                  <button
                    aria-expanded="false"
                    aria-controls={`submenu-${entry.key}`}
                    class:list={[
                      'c-list__item-link c-dropdown-button',
                      { 'c-active': Astro.url.pathname.includes(entry.url) },
                    ]}
                  >
                    {entry.key}
                    <Icon name="mdi--caret" class="c-drop-icon" />
                  </button>
                ) : (
                  // If entry does not have children in navData.json, create an anchor
                  <a
                    href={entry.url}
                    class:list={[
                      'c-list__item-link',
                      { 'c-active': Astro.url.pathname === entry.url },
                    ]}
                    aria-current={
                      Astro.url.pathname === entry.url ? 'page' : undefined
                    }
                    target={entry.isAdmin ? '_blank' : undefined}
                    rel={entry.isAdmin ? 'noopener noreferrer' : undefined}
                  >
                    {entry.key}
                  </a>
                )}

                {entry.children?.length > 0 && (
                  // If entry has children in navData.json, create a drop down menu
                  <ul id={`submenu-${entry.key}`} class="c-drop-ul">
                    {entry.children.map(child => (
                      <li class="c-drop-li">
                        <a
                          href={child.url}
                          class="c-list__item-link c-drop-link"
                          aria-current={
                            Astro.url.pathname === child.url
                              ? 'page'
                              : undefined
                          }
                        >
                          {child.key}
                        </a>
                      </li>
                    ))}
                  </ul>
                )}
              </li>
            ))
          }
        </ul>
      </div>
    </nav>
    <!--Dark Mode toggle: remove component if you don't want to enable a dark mode toggle-->
    <DarkModeToggle />
  </div>
</header>

<style lang="less">
  @import '../styles/components/_header.less';
</style>
