---
import Button from '@components/ui/Button.astro';

interface Props {
  eyebrow: string;
  title: string;
  summary: string;
  primaryLabel: string;
  primaryHref: string;
  secondaryLabel: string;
  secondaryHref: string;
  desktopImage: string;
  mobileImage: string;
}

const {
  eyebrow,
  title,
  summary,
  primaryLabel,
  primaryHref,
  secondaryLabel,
  secondaryHref,
  desktopImage,
  mobileImage,
} = Astro.props as Props;
---

<section id="hero" class="home-hero" aria-labelledby="home-hero-title">
  <picture class="home-hero__media" aria-hidden="true">
    <source media="(max-width: 600px)" srcset={mobileImage} />
    <img
      src={desktopImage}
      alt=""
      width="3838"
      height="1632"
      sizes="100vw"
      decoding="async"
      fetchpriority="high"
    />
  </picture>
  <div class="home-hero__overlay"></div>
  <div class="home-hero__content" data-reveal="intro">
    <p class="home-hero__eyebrow">{eyebrow}</p>
    <h1 id="home-hero-title">{title}</h1>
    <p class="home-hero__summary">{summary}</p>
    <div class="home-hero__actions">
      <Button href={primaryHref}>{primaryLabel}</Button>
      <Button href={secondaryHref} variant="ghost" class="home-hero__secondary"
        >{secondaryLabel}</Button
      >
    </div>
  </div>
</section>

<style lang="less">
  .home-hero {
    min-height: min(48rem, 100dvh);
    overflow: hidden;
    position: relative;
    display: grid;
    align-items: end;
    isolation: isolate;

    &__media,
    &__media img,
    &__overlay {
      inset: 0;
      position: absolute;
      width: 100%;
      height: 100%;
    }
    &__media {
      z-index: -2;
    }
    &__media img {
      object-fit: cover;
    }
    &__overlay {
      background: linear-gradient(
        90deg,
        rgb(var(--color-photo-scrim-rgb) / 88%),
        rgb(var(--color-photo-scrim-rgb) / 58%) 58%,
        rgb(var(--color-photo-scrim-rgb) / 28%)
      );
      z-index: -1;
    }
    &__content {
      width: min(100% - 2rem, 75rem);
      margin: 0 auto;
      padding: 6rem 0 clamp(3.5rem, 8vw, 6rem);
      color: var(--color-text-inverse);
    }
    &__eyebrow {
      color: var(--color-hero-eyebrow);
      font-size: 0.875rem;
      font-weight: 700;
      letter-spacing: 0.08em;
      margin-bottom: 1rem;
      text-transform: uppercase;
    }
    h1 {
      font-family: var(--font-editorial);
      color: inherit;
      font-size: clamp(2.75rem, 6.5vw, 5.25rem);
      font-weight: 700;
      letter-spacing: -0.035em;
      line-height: 1;
      max-width: 10ch;
    }
    &__summary {
      color: inherit;
      font-size: clamp(1.05rem, 2vw, 1.25rem);
      line-height: 1.55;
      margin-top: 1.5rem;
      max-width: 34rem;
    }
    &__actions {
      display: flex;
      flex-wrap: wrap;
      gap: 0.75rem;
      margin-top: 2rem;
    }
    :global(.home-hero__secondary) {
      --button-ghost-border: rgb(var(--color-inverse-rgb) / 70%);
      --button-ghost-color: var(--color-text-inverse);
      --button-ghost-fill: rgb(var(--color-inverse-rgb) / 16%);
      --button-ghost-hover-color: var(--color-text-inverse);
    }
  }
  @media (max-width: 47.9375rem) {
    .home-hero__overlay {
      background: linear-gradient(
        0deg,
        rgb(var(--color-photo-scrim-rgb) / 90%),
        rgb(var(--color-photo-scrim-rgb) / 38%)
      );
    }
  }
</style>
