---
import Modal from '@components/ui/Modal.astro';

const cookieModalId = 'cookie-settings-modal';
const cookieModalTitleId = 'cookie-settings-title';
---

<div id="cookie-banner" class="cookie-banner" style="display: none;">
  <div class="cookie-banner-content">
    <div class="cookie-banner-text">
      <h3>Gestión de Cookies</h3>
      <p>
        Este sitio web utiliza cookies para mejorar su experiencia de
        navegación. Las cookies analíticas (Google Analytics y Sentry) nos
        ayudan a entender cómo interactúan los visitantes con el sitio y
        detectar errores técnicos.
      </p>
      <p class="cookie-banner-link">
        <a href="/politica-de-cookies" target="_blank"
          >Más información sobre nuestras cookies</a
        >
      </p>
    </div>
    <div class="cookie-banner-buttons">
      <button id="accept-all-cookies" class="cookie-btn cookie-btn-accept">
        Aceptar todas
      </button>
      <button
        id="accept-necessary-only"
        class="cookie-btn cookie-btn-necessary"
      >
        Solo necesarias
      </button>
      <button id="cookie-settings" class="cookie-btn cookie-btn-settings">
        Configurar
      </button>
    </div>
  </div>
</div>

<!-- Cookie Settings Modal -->
<Modal
  id={cookieModalId}
  ariaLabel="Configuración de Cookies"
  labelledBy={cookieModalTitleId}
  class="cookie-modal"
  dialogClass="cookie-modal-content"
  closeButton={false}
>
  <div class="cookie-modal-header">
    <h3 id={cookieModalTitleId}>Configuración de Cookies</h3>
    <button
      id="close-cookie-modal"
      class="cookie-modal-close"
      type="button"
      aria-label="Cerrar configuración de cookies"
      data-modal-close
    >
      &times;
    </button>
  </div>
  <div class="cookie-modal-body">
    <div class="cookie-category">
      <div class="cookie-category-header">
        <input type="checkbox" id="necessary-cookies" checked disabled />
        <label for="necessary-cookies">
          <strong>Cookies Necesarias</strong>
        </label>
      </div>
      <p class="cookie-category-description">
        Estas cookies son esenciales para el funcionamiento del sitio web y no
        se pueden desactivar.
      </p>
    </div>

    <div class="cookie-category">
      <div class="cookie-category-header">
        <input type="checkbox" id="analytics-cookies" />
        <label for="analytics-cookies">
          <strong>Cookies Analíticas</strong>
        </label>
      </div>
      <p class="cookie-category-description">
        Estas cookies nos permiten analizar el uso del sitio web para mejorar su
        rendimiento y experiencia de usuario, además de detectar errores de
        forma proactiva. Utilizamos Google Analytics y Sentry para estos
        propósitos.
      </p>
    </div>
  </div>
  <div class="cookie-modal-footer">
    <button id="save-cookie-preferences" class="cookie-btn cookie-btn-accept">
      Guardar preferencias
    </button>
  </div>
</Modal>

<style>
  .cookie-banner {
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    background: var(--color-surface-inverse);
    color: var(--color-text-inverse);
    padding: 1rem;
    box-shadow: var(--shadow-lg);
    z-index: 9999;
    font-family:
      -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  }

  .cookie-banner-content {
    max-width: 1200px;
    margin: 0 auto;
    display: flex;
    align-items: center;
    gap: 2rem;
    flex-wrap: wrap;
  }

  .cookie-banner-text {
    flex: 1;
    min-width: 300px;
  }

  .cookie-banner-text h3 {
    margin: 0 0 0.5rem 0;
    font-size: 1.1rem;
    color: var(--color-text-inverse);
  }

  .cookie-banner-text p {
    margin: 0.5rem 0;
    font-size: 0.9rem;
    line-height: 1.4;
    color: var(--color-text-inverse);
  }

  .cookie-banner-link a {
    color: var(--color-footer-link);
    text-decoration: underline;
  }

  .cookie-banner-buttons {
    display: flex;
    gap: 0.75rem;
    flex-wrap: wrap;
  }

  .cookie-btn {
    padding: 0.75rem 1.5rem;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    font-size: 0.9rem;
    font-weight: 500;
    transition: all 0.3s ease;
    white-space: nowrap;
  }

  .cookie-btn-accept {
    background: var(--color-territory-surface);
    color: var(--color-on-territory);
  }

  .cookie-btn-accept:hover {
    background: color-mix(
      in srgb,
      var(--color-territory-surface) 88%,
      var(--color-surface-inverse)
    );
  }

  .cookie-btn-necessary {
    background: var(--color-surface);
    color: var(--color-text);
  }

  .cookie-btn-necessary:hover {
    background: var(--color-surface-muted);
  }

  .cookie-btn-settings {
    background: transparent;
    color: var(--color-text-inverse);
    border: 1px solid var(--color-border-strong);
  }

  .cookie-btn-settings:hover {
    background: color-mix(in srgb, var(--color-text-inverse) 12%, transparent);
  }

  /* Modal styles */
  .cookie-modal {
    --modal-padding: 1rem;
    --modal-overlay-background: rgb(var(--color-overlay-rgb) / 60%);
    --modal-dialog-background: var(--color-surface);
    --modal-dialog-radius: 8px;
    --modal-dialog-padding: 0;
    --modal-dialog-shadow: var(--shadow-lg);
    --modal-z-index: 10000;
  }

  .cookie-modal-content {
    max-width: 600px;
    width: 100%;
    max-height: 80vh;
    overflow-y: auto;
    color: var(--color-text);
    background: var(--modal-dialog-background, var(--color-surface));
    border-radius: 8px;
  }

  .cookie-modal-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1.5rem;
    border-bottom: 1px solid var(--color-border);
  }

  .cookie-modal-header h3 {
    margin: 0;
    font-size: 1.25rem;
  }

  .cookie-modal-close {
    background: none;
    border: none;
    font-size: 1.5rem;
    cursor: pointer;
    color: var(--color-text-muted);
    padding: 0;
    width: 30px;
    height: 30px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .cookie-modal-close:hover {
    color: var(--color-text);
  }

  .cookie-modal-body {
    padding: 1.5rem;
  }

  .cookie-category {
    margin-bottom: 2rem;
  }

  .cookie-category:last-child {
    margin-bottom: 0;
  }

  .cookie-category-header {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: 0.5rem;
  }

  .cookie-category-header input[type='checkbox'] {
    width: 18px;
    height: 18px;
  }

  .cookie-category-header label {
    margin: 0;
    cursor: pointer;
  }

  .cookie-category-description {
    margin: 0;
    font-size: 0.9rem;
    line-height: 1.4;
    color: var(--color-text-muted);
    margin-left: 2.25rem;
  }

  .cookie-modal-footer {
    padding: 1.5rem;
    border-top: 1px solid var(--color-border);
    text-align: right;
  }

  /* Responsive design */
  @media (max-width: 768px) {
    .cookie-banner-content {
      flex-direction: column;
      align-items: stretch;
      gap: 1rem;
    }

    .cookie-banner-buttons {
      justify-content: stretch;
    }

    .cookie-btn {
      flex: 1;
      text-align: center;
    }

    .cookie-modal {
      --modal-padding: 0.5rem;
    }

    .cookie-category-description {
      margin-left: 0;
      margin-top: 0.5rem;
    }
  }
</style>
